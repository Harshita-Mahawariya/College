<h1> Student </h1>
<table>
	<tr><th>Name</th><th>Course</th></tr>
		<tr><td><%= @student.name%></td>
		<td><%= @student.course.name%></td>
		<td><%= link_to 'Edit' , edit_student_path%></td>
		<td><%= link_to 'Delete' , student_path(@student), method: :delete%></td></tr>
</table>